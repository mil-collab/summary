# images

### Dockerfile
Vault image.

### post-install.sh
Example for setting up a Vault-K8s auth for a cluster.
> This needs to run as a post-install webhook of local cluster chart.

### pre-delete.sh
Example for deleting the auth.
> This needs to run as a pre-delete webhook of local cluster chart.
